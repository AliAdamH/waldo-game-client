import React, { useEffect } from 'react';

const Timer = (props) => {
  useEffect(() => {
    // on mount start doing things.
  }, []);

  return (
    <>
      <p>Hello from timer</p>
    </>
  );
};

export default Timer;
